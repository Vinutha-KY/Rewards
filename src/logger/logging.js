const logger = {
    logInfo: (message) => {
      console.log("INFO: " + message);
    },
    logError: (message) => {
      console.error("ERROR: " + message);
    }
  };
  
  export default logger;
  